version: '3.9'

services:
  frontend:
    build: .
    container_name: frontend-app
    ports:
      - "3000:80" # mapeia porta 80 do Nginx para 3000 do host
    environment:
      - REACT_APP_API_URL=http://localhost:5000
    depends_on:
      - api
